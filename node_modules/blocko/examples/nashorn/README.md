Compile blocko for Homer (nashorn)
----------------------------------

1) install Browserify (only first)
	"npm install -g browserify"
2) run Browserify
	"browserify nashorn-blocko.js -o _nashorn-blocko.out.js"
